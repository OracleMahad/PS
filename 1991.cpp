#include <iostream>
#include <cstdlib>
#include <algorithm>
#include <cstring>
#include <vector>
#include <climits>

using namespace std;
int N;
struct node
{
    char par;
    struct  node *left=NULL;
    struct  node *right=NULL;

};


int main(){
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    struct node nd[27];
    cin >> N; 
    for(int i=0;i<N;i++){
        char a, b, c;
        cin a >> b >> c;
        
    }
    
    
}
